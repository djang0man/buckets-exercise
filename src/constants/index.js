export const CAPACITY_A = 3;
export const CAPACITY_B = 5;
export const TARGET_LEVEL = 4;
export const BUCKET_DEFAULTS = [
  { id: btoa(Math.random()), level: 0, capacity: CAPACITY_A },
  { id: btoa(Math.random()), level: 0, capacity: CAPACITY_B }
]

